window.onload = function() {
            // Ottieni la larghezza dello schermo
            var screenWidth = window.innerWidth;

            // Imposta la larghezza massima del body
            document.body.style.maxWidth = screenWidth + "px";
        };